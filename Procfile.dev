### Django runs on this
web: python manage.py runserver 0:8000

### Celery workers

## beat: Trigger tasks for all queues, and processes the ones in queue 'celery'
beat: celery worker -B --app=lily.celery --loglevel=info -Q celery -n beat.%h -c 1

## worker: Execute tasks in queue 'queue1'
worker1: celery worker --loglevel=info --app=lily.celery -Q queue1 -n worker1.%h -c 1 -P eventlet

## worker: Execute tasks in queue 'queue2'
worker2: celery worker --loglevel=info --app=lily.celery -Q queue2 -n worker2.%h -c 1 -P eventlet

## worker: Execute tasks in queue 'queue3'
worker3: celery worker --loglevel=info --app=lily.celery -Q queue3 -n worker3.%h -c 1 -P eventlet

### Run Elasticsearch locally (preferrably from an extracted Elasticsearch package tar).
# search: elasticsearch
