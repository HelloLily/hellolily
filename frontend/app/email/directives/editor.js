angular.module('app.email.directives').component('editor', {
    templateUrl: 'email/directives/editor.html',
    controller: EditorController,
    bindings: {
    },
    transclude: true,
});

function EditorController() {

}
